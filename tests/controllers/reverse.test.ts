//import { Options } from '../../src/controllers/reverse.ts';

import { expect } from 'chai';

describe('Reverse tests', () => {
    it('Simple reverse', () => {
        expect(false).to.be.false;
    });
});
